import { style } from '@vanilla-extract/css';

export const BookmarkHeaderLayout = style({
  display: 'flex',
  justifyContent: 'space-between',
  gap: '8px',
});

export const logoSectionLayout = style({
  display: 'flex',
});
